  float d;
  float midPoint;
  
void setup()
{
  size(500, 500);
  d = width;
  midPoint = width/2;
  noFill();
}

void draw()
{
/*
  for(int i = 100; i <= 180; i += 20)
  {
    line(i, 10, i, 100);
  }
  
  for(float x = 0; x < 5; x ++)
  {
    float i = 100 + (x * 20);
    line(i, 50, i, 100);
  }
  int x = 10;
  while(x <= 100)
  {
    line(x, 10, x, 100);
    x += 20;
  }
  int line = 0;
  while(line < 5)
  {
    int x = 100 + (line * 20);
    line(x, 10, x, 100);
    line ++;
  }
  
  float r = d / 10;
  while(r <= d)
  {
    ellipse(midPoint, midPoint, r, r);
    r += 10;
  }
  */
  
  
  
}
